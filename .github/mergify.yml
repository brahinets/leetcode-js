pull_request_rules:
  - name: "Automatic merge when CI passes"
    conditions:
      - "base=main"
      - "check-success=verification"
    actions:
      merge:
        method: merge

  - name: "Delete head branch after merge"
    conditions:
      - merged
    actions:
      delete_head_branch:
